#include <stdio.h>

int main() {
    // Write C code here
    int n;
    printf("Enter the number n:");
    scanf("%d",&n);
    if (n%2==0){
    printf("The number is prime %d.",n);
    }
    else{
    printf("The number is not prime %d.",n);
    }
    return 0;
