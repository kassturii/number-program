#include <stdio.h>

int main() {
    // Write C code here
    int i ,n ,sum=0;
    printf("Enter the sum of the value of n:");
    scanf("%d",&n);
    for (i=0;i<=n;i++){
    sum=sum+i;
    }
    printf("Sum = %d",sum);

    return 0;
}
